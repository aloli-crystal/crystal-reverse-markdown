name: CI

on:
  push:
    branches: [production]
  pull_request:
    branches: [production]

jobs:
  test:
    runs-on: ubuntu-latest

    strategy:
      matrix:
        crystal: ["1.19.0", "1.19.1"]

    steps:
      - uses: actions/checkout@v4

      - uses: crystal-lang/install-crystal@v1
        with:
          crystal: ${{ matrix.crystal }}

      - name: Install dependencies
        run: shards install

      - name: Check formatting
        run: crystal tool format --check src/ spec/

      - name: Run tests
        run: crystal spec

      - name: Run linter
        run: bin/ameba

      - name: Build CLI
        run: crystal build bin/reverse-markdown.cr -o bin/reverse-markdown
