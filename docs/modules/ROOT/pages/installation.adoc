= Installation

== Add the Dependency

Add reverse-markdown to your `shard.yml`:

[source,yaml]
----
dependencies:
  reverse-markdown:
    github: aloli-crystal/reverse-markdown
    version: "~> 3.0.2.4"
----

Then run:

[source,bash]
----
shards install
----
